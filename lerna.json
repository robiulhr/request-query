{
  "version": "independent",
  "npmClient": "npm",
  "useWorkspaces": true,
  "packages": ["packages/*"],
  "command": {
    "publish": {
      "registry": "https://registry.npmjs.org/",
      "ignoreChanges": ["**/*.md", "**/*.json", "**/*.yml", "**/*.yaml", "**/*.html", "docs/**"],
      "npmClient": "npm"
    }
  }
}
